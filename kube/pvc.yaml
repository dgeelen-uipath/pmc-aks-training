{
  "apiVersion": "v1",
  "kind": "PersistentVolumeClaim",
  "metadata": {
    "name": "todo-db"
  },
  "spec": {
    "accessModes": [
      "ReadWriteMany"
    ],
    "storageClassName": "azurefile",
    "resources": {
      "requests": {
        "storage": "100Gi"
      }
    }
  }
}